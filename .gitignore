# Build artifacts (the release zip is produced by CI / packaging, never committed)
*.zip

# Local WPCS / PHPCS verification tooling (see CLAUDE.md → Dev / verification tooling)
/phpcs.phar
/phpcbf.phar
/wpcs/
/phpcsutils/
/phpcsextra/

# Dev/agent-context notes — kept locally, not published to the repo
/CLAUDE.md

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
